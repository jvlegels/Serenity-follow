# Optional. Without this token the app runs in mock mode.
X_BEARER_TOKEN=

# Optional. Defaults to aleabitoreddit.
X_USERNAME=aleabitoreddit

# Optional. Use this if you already know the numeric X user id.
X_USER_ID=

# Optional. Defaults to 8787 locally and is usually set by hosting providers in production.
PORT=8787
